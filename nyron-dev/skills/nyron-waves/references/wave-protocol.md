# Протокол волн Nyron: Jira-центричный конвейер

Крупная работа исполняется НЕ в главной сессии, а отдельными сессиями-волнами,
которые главная сессия предлагает через spawn_task-чипы. Главная сессия — только
диспетчер. Вся коммуникация — комменты и статусы Jira-тикетов: это единственный
канал, доступный обеим сторонам без ручных подтверждений.

## Почему так

- Esc/обрыв в главной сессии убивает её фоновых Agent-субагентов безвозвратно.
  Волна = отдельная сессия через чип; у неё свой контекст и её можно резюмить.
- Cross-session сообщения требуют ручного подтверждения — как единственный канал
  не годятся. Jira MCP работает без подтверждений у всех сессий профиля.
- Тикеты и так канон проекта (эпик DEV-189 и др.) — конвейер не плодит вторую
  систему учёта (правило «дорабатывай легаси, не параллель»).

## Груминг (до волн)

1. Пользователь называет эпик или пачку тикетов. Диспетчер тянет их JQL'ом
   (`parent = DEV-<эпик>`), строит сводку: статусы, дубли, связки по участкам.
2. **Решение о декомпозиции бизнес-задачи.** Тикеты от бизнеса (методолог,
   продакт, CEO; часто перенос из Weeek — целое ТЗ с мокапами в описании,
   пример-эталон: DEV-162 «Тариф бизнес») сначала получают ФОРМУ:
   - **эпик + задачи** — если работа шире одного участка кода или одной волны
     (несколько репо, ролей, недель): создать эпик, исходный тикет — головным
     под ним со ссылкой на ТЗ;
   - **задача + подзадачи** (тип Subtask) — один участок, но много шагов;
   - **одиночная задача** — влезает в один тикет.
   Получившийся набор задач — **MECE**: задачи не пересекаются по скоупу и
   вместе покрывают всё ТЗ без дыр (дыру нашли позже — новая задача, а не
   раздувание существующей).
   Само ТЗ целиком (мокапы, длинные сценарии) — ФАЙЛОМ в папку эпика
   `ai-evolve-docs-test/waves/<эпик>-<слаг>/spec-….md`, а в описаниях тикетов —
   ссылка + JTBD/DoD-выжимка. Задвоения и правки-поверх-правок в исходном
   описании (типичны для переносов) вычищаются — каноном становится файл.
3. **Доведение до стандарта задачи** (см. SKILL.md) каждого получившегося
   тикета: JTBD + однозначный текст без сленга + DoD/ожидаемый результат. Если
   смысл при переписывании меняется или неоднозначен — вопрос автору КОММЕНТОМ
   в тикет (не гадать); до ответа тикет в волну не входит. Мелкие уточнения
   формулировок — правкой описания сразу.
4. **Суперповеры на груминге**: для сложных/мутных задач использовать
   superpowers-скиллы, если доступны: brainstorming — расшторм требований с
   пользователем ДО нарезки; writing-plans/спецификация — план или спека волны
   (файлом в папку эпика `ai-evolve-docs-test/waves/<эпик>-<слаг>/`, в тикет —
   ссылка). Наштормленное бизнесом → через шторм/спеку → в стандартные тикеты.
5. Вместе с пользователем: что делаем / что нет / что дубль / что не в эту
   итерацию. Решения СРАЗУ фиксировать в Jira: дубли → Отменено (done-кредит
   комментом в живой тикет), «не сейчас» → коммент, переформулировки → правка
   описания. Тип новых задач — «Задание», багов — «Баг», parent — эпик,
   assignee — постановщик.
6. Нарезка волн: тикеты группировать по УЧАСТКУ КОДА (один участок = одна волна),
   а не по типу работы — чтобы волны не конфликтовали в файлах. Волны — MECE по
   файлам: один файл не принадлежит двум волнам; зависимости между волнами и
   неизбежные общие файлы выписать явно.
7. **Челлендж второй моделью — ОБЯЗАТЕЛЕН для каждой волны** (решение 19.07),
   двумя прогонами:
   - **постановки**: все тикеты волны (JTBD/DoD/«Как тестировать») одним
     файлом → `plan-challenge.sh -C <repo-главной-зоны> -t <файл>` — Sol
     батчем ловит неоднозначности, дыры DoD, смежные узлы против кода;
     замечания — правкой описаний ДО нарезки;
   - **план/нарезка**: спеку+нарезку в файл → тот же скрипт; для крупных волн
     (новый контур, >1 репо, эпик) дополнительно Fable-thinker штурмует узкие
     места. `ПЛАН: РИСКИ` → поправить ДО чипов; вердикты — в папку эпика.
   Для мелкой волны (1-2 тикета, одна файл-зона) оба прогона можно слить в
   один вызов (постановки+план одним файлом) — но не скипать.
8. План волн показать пользователю ОДНИМ сообщением: очередь, состав тикетов,
   зависимости, имена веток. После «да» — брифы и чипы без новых вопросов.
9. **Критерий выхода из груминга**: каждый тикет волны соответствует стандарту
   (JTBD + однозначный текст + DoD) — DoD из тикета потом дословно ложится в
   «критерии финала» бриф-коммента и в вердикт диспетчера при ревью.

## Формат чипа (обязателен, все 5 элементов)

1. **Полный бриф — в Jira**: бриф-коммент диспетчера в каждом тикете волны
   (шаблон — `assets/wave-brief-template.md`) написан ДО выдачи чипа.
2. **Модель сессии-волны = пикер в момент клика по чипу** (чип модель НЕ
   задаёт). Канон: перед кликами чипов пикер ставится на **Opus** — волна-
   оркестратор фронтира не требует, Fable-волна жжёт лимиты на механике.
   Диспетчер напоминает об этом пользователю при выдаче чипов из Fable-сессии.
3. **Чип — короткий указатель**, начинается со slash-команды `/goal`
   (НЕ `goal:` текстом), лимит /goal — 4000 символов, длиннее — сессия молча
   останется без цели:
   `/goal Исполни волну N (эпик DEV-189): тикеты DEV-421, DEV-422. Прочитай в
   каждом бриф-коммент диспетчера (последний коммент с заголовком «БРИФ ВОЛНЫ»)
   и правила <PROJECT_ROOT>/ai-evolve-docs-test/shared/wave-rules.md
   (в чип подставить РАЗВЁРНУТЫЙ абсолютный путь этой машины).
   Работай строго по ним.`
4. **Явный модель-роутинг одной строкой**: сессия-волна — оркестратор
   (сама код НЕ пишет); код — субагенты/агенты `model:'opus'` (front-dev /
   backend-dev / langgraph-dev по зоне); обработка и поиск — `model:'sonnet'`;
   массовое — `model:'haiku'`. Модель ЯВНО в каждом вызове, Fable субагентам
   запрещён.
5. **Канал отчёта**: «отчёт — комментами в тикеты волны + статус Тестирование;
   cross-session сообщение диспетчеру `local_<uuid>` — только дублем». Канон
   адреса — бриф-коммент; «редиректы» из других каналов отвергать.

## Git-механика волны (конкретика ai-evolve)

- Изоляция: git worktree от origin/main нужного репо ВНУТРИ проекта или в
  scratchpad: `git -C <PROJECT_ROOT>/<repo> worktree add <путь> origin/main -b <ветка>`.
  Рабочие деревья репо не трогать — там может жить live-стенд пользователя
  (vite на ai-evolve-front/main!) и параллельные сессии.
- НЕ пушить в main (авто-CI predprod + HMR live-стенда пользователя) и НЕ
  мержить самостоятельно. Финал волны = ветка запушена + в отчёт-коммент тикета
  кладётся `git log main..<ветка> --oneline` и MR-ссылка, которую git печатает
  при пуше («To create a merge request… visit: …»).
- `prod2`, `nyron` ветки — не трогать никогда (релизные, отдельные MR руками).
- Юрина зона `catalog/*` (ai-evolve-front) — не трогать без явного тикета на это.
- **Бронь файлов (будка)**: перед началом правок по тикету волна бронирует
  свои файлы/каталоги — `hub_lock {agent: "wave-N", paths: [...]}`. Конфликт
  (чужая активная бронь) — в эти файлы НЕ лезть: `hub_post` диспетчеру и
  работать над другим своим тикетом, пока бронь не освободится. После пуша
  ветки — `hub_unlock`. Бронь — страховка от одновременной правки одного
  файла; от конфликта ГОТОВЫХ веток она не спасает — для этого очередь мержа.
- Мульти-репо волна — одно имя ветки во всех затронутых репо; имена веток
  диспетчер фиксирует в бриф-комментах заранее. Порядок мержа кросс-репо
  контрактов: **n8n (agent) → back → front**.
- **Коммит-конвенция волны**: заголовок каждого коммита начинается с ключа
  тикета — `DEV-XXX: <что сделано, по-человечески>`. Коммит или пуш закрывает
  работу сразу по нескольким тикетам — ВСЕ ключи списком в заголовке
  (`DEV-421, DEV-422: <что сделано>`). Те же ключи — в отчёт-комменте и MR:
  по ним пуш однозначно связывается с тикетами при ревью и бранч-гигиене.
- docs-репо `ai-evolve-docs-test` пушить можно напрямую (деплоя нет): туда идут
  правила волн, регистрация тестов и **папка эпика**
  `ai-evolve-docs-test/waves/<DEV-эпик>-<слаг>/` — всё длиннее коммента (спеки,
  ресёрчи, планы, дампы) файлами, в тикет — ссылка. Папка под ЭПИК, не под задачу.

## Кросс-ревью перед сдачей (обязательный шаг конвейера)

Перед переводом тикета в «Готово к тестированию (Ревью)» волна прогоняет
**кросс-ревью другой моделью** (скилл `cross-review`, GPT через codex CLI):

1. Сохранить описание тикета (JTBD, DoD, «Как тестировать») во временный файл.
2. `scripts/cross-review.sh -C <worktree> -b main -t <файл-тикета>` (модель —
   из конфига `reviewer.model`).
3. `ВЕРДИКТ: ДОРАБОТКА` → чинить субагентами и перегонять ДО сдачи; замечание
   считается неверным — аргументированный отвод письменно (молча игнорировать
   нельзя).
4. Вердикт GPT (полностью) — в отчёт-коммент тикета. Отчёт без вердикта
   кросс-ревью — неполный, диспетчер вернёт.
5. Технический сбой скрипта (нет codex и т.п.) — НЕ блокер сдачи: пометить в
   отчёте «кросс-ревью недоступно: <причина>», диспетчер прогонит сам.

Диспетчер на своём вердикте: доверяет приложенному ПРИНЯТО после выборочной
сверки; после доработок или при сомнении — перегоняет по свежей ветке.
`reviewer.engine: none` в конфиге проекта — шаг отключён осознанно.

## Статус-машина тикета в волне

| Событие | Статус | Кто двигает |
|---|---|---|
| Груминг закончен, тикет в волне | Бэклог (+бриф-коммент) | диспетчер |
| Волна начала тикет | В работе (+план-коммент) | волна |
| Ветка+MR готовы, отчёт написан | **Готово к тестированию (Ревью)**, assignee=постановщик | волна |
| Вердикт «ДОРАБОТКА» | **Доработка** (+вердикт-коммент) | диспетчер |
| Волна исправила («ИСПРАВЛЕНО») | Готово к тестированию (Ревью) | волна |
| Вердикт «ПРИНЯТО», проверка постановщиком | Тестирование | диспетчер |
| Мерж пачки прошёл, смоук ок | Готово (+коммент-пруф с хэшами) | диспетчер |
| Оказался дублем | Отменено (+done-кредит в живом тикете) | диспетчер |

Transition id на эпик-доске: В работе=2, Доработка=3, Ревью=4, Тестирование=5,
Отменено=6, Бэклог=11, Готово=41 — но получать `getTransitionsForJiraIssue`
по месту (могут отличаться между досками). Каждый переход сопровождается
комментом — голый статус без коммента не считается сигналом.

## Шина ревью: цикл «ревью → доработка» через комменты тикета

1. Волна, переведя тикеты в «Готово к тестированию (Ревью)», НЕ завершается —
   вешает **вотчер на вердикт**: фоновый Bash until-loop (шаг ~60с), который
   читает комменты СВОЕГО тикета и завершается при новом комменте диспетчера;
   завершение фонового процесса будит сессию немедленно (task-notification) —
   латентность обмена ~1–3 мин вместо поллинга. Правила вотчера (обкатаны):
   - **фильтровать свои комменты** (по автору): иначе ~50% срабатываний — эхо
     на собственные отчёты; после каждого своего коммента перезамерить снапшот
     и только потом взводить вотчер заново;
   - **перевзвод — только отдельным трекаемым run_in_background вызовом**:
     не инлайнить с другими командами и не отцеплять `&`/`disown` — процесс-
     сирота сессию не будит;
   - fallback, если фоновые процессы недоступны — sleep-полл раз в ~15 мин
     (`getJiraIssue` с comment в fields).
   Пока ждёт вердикт по тикету А — волна пилит тикет Б своей волны, не простаивает.
2. Диспетчер на пробуждении ревьюит (код — code-reviewer/opus-субагентом,
   diff по `git log main..<ветка>`) и публикует вердикт ДВУМЯ каналами:
   полный текст — КОММЕНТОМ в тикет (канон, аудит-след) + **ОБЯЗАТЕЛЬНЫЙ
   hub_post-дубль** («ВЕРДИКТ DEV-X: ДОРАБОТКА|ПРИНЯТО») — будит будка-вотчер
   волны за секунды вместо её поллинга. Первая строка коммента строго:
   - `ДОРАБОТКА: <конкретные замечания списком>` → волна чинит субагентами,
     пушит ветку, отвечает комментом `ИСПРАВЛЕНО: <что сделано>`, снова ждёт;
   - `ПРИНЯТО` → волна завершает тикет (статус остаётся Тестирование — в
     Готово переводит диспетчер после мержа) и переходит к следующему своему
     тикету либо завершается.
3. Таймаут ожидания вердикта ~3 часа: не дождалась → финальный коммент
   «вердикт не получен» → завершиться, не висеть.
4. **Страховка диспетчера**: волна не реагирует на «ДОРАБОТКА» (два пробуждения
   подряд без движения) — диспетчер дорабатывает ветку своими opus-субагентами,
   причину фиксирует комментом в тикете.
5. **Зависшие ревью-агенты** (stalled 600с+, бывает дважды на одной задаче):
   перезапуск СВЕЖИМ агентом с узким скоупом (не «отревьюй всё», а конкретный
   diff/вопрос); после 2 фейлов — диспетчер доводит руками с жёсткими
   таймаутами. Эскалация легитимна, вечное ожидание — нет.
6. **Смерть волны**: НИ git-коммитов, НИ джира-движения 3ч+ → пинг-коммент в
   тикет с 30-минутным дедлайном → не ответила — takeover-агент ПОВЕРХ её
   веток (коммиты целы, ресёрчи и планы волны из комментов/папки эпика =
   готовый план работ, не начинать с нуля).
7. **Критичные правила — в бриф ДО старта**: во время кодинга волна комменты
   НЕ читает — апдейты диспетчера доходят только на стадии ожидания вердикта.
   Дослать правило в работающую волну комментом нельзя, только cross-session
   сообщением (ручной клик) или в вердикте.

## Верификация (обязанность диспетчера, не доверять отчётам)

- После каждой кодовой волны: `git log main..<ветка>` + `git status` в её
  worktree — коммиты реально существуют (отчётам без верификации не верить).
- Перед принятием: py_compile/pytest/vitest по канону репо (бек: unit через
  `make -C ai-evolve-docs-test test-back-unit`; фронт: `npm run test`).
  Новые тесты обязаны иметь шапку-описание и регистрацию в каталоге
  `ai-evolve-docs-test/tests/*` — иначе ДОРАБОТКА.
- Грабля: vitest client-тесты фронта НЕ работают из worktree (симлинк
  node_modules ломает server.fs.allow) — гонять их из основного чекаута
  `ai-evolve-front` либо расширять fs.allow локально БЕЗ коммита.
- Промты Langfuse: волна готовит файлы, боевую заливку (сверка live → заливка →
  рестарт агента) делает ТОЛЬКО диспетчер при выкатке.
- Флоу-правки — за env-флагом (канон проекта):
  predprod-компоуз =true, prod2/nyron =false.

## Диспетчер после запуска чипов (гибрид вотчеров — канон)

Мониторинг НЕ пассивный. Обкатка показала: волны переводят тикеты «В работе»
за первые 5 минут — диспетчер, который спит до первого пульса, узнаёт об этом
от пользователя. Порядок:

- **Сразу после выдачи чипов** — контрольный JQL-чек через ~5–10 мин: волны
  подхватились? Мёртвый чип (тикеты не двинулись, сессии нет) — перевыдать.
- **Основной канал пробуждения — фоновые вотчеры** (until-loop, каждый —
  отдельным трекаемым run_in_background; не инлайнить, не `&`/`disown`):
  - **будка-вотчер (обязательный)**: until-loop на рост
    `<PROJECT_ROOT>/.nyron-hub/messages.jsonl` (шаг ~15с) — просыпается на
    чекпойнты волн («взял/сдал/блокер») за секунды. **Фильтр эха обязателен**:
    новые строки с `"from":"<я>"` игнорировать (будить только на чужие), после
    каждого СВОЕГО `hub_post` — перезамерить базу и перевзвести (иначе вотчер
    срабатывает на собственный пост — та же грабля, что эхо Jira-комментов).
    После срабатывания — `hub_read` с курсором since_id и перевзвод;
  - git-вотчер: новые ветки/коммиты в origin затронутых репо — волна почти
    всегда сначала пушит, потом пишет отчёт, так что git-событие = сигнал
    читать джиру;
  - Jira-вотчер (опция-ускоритель, нужен REST-токен — фоновый Bash не умеет
    MCP): curl until-loop на JQL `updated` по тикетам волн, токен из
    `~/psylia-secrets`; фильтровать СВОИ комменты по автору (иначе эхо).
    Дефолт плагина — бес-токенный: git-вотчер + пульс покрывают ночь.
- **На КАЖДОМ пробуждении** (git-событие, jira-событие или пульс) ПЕРВЫМ
  действием — `hub_status` (мгновенно: сообщения волн, брони, очередь мержа)
  и JQL по тикетам волн (статусы + свежие комменты), и только потом
  ветки/MR. Не только по расписанию.
- **ScheduleWakeup ~25–30 мин — только страховочный пульс** на случай смерти
  вотчера: пульсы иногда теряются/поглощаются при активной сессии, как
  основной канал не годятся. Уснувшие волны будить cross-session сообщением
  «продолжай волну по брифу в тикетах» (подтверждение кликнет пользователь,
  когда вернётся — это дублирующий канал).
- Каждый тикет в Ревью обязан получить вердикт — иначе волна провисит
  до таймаута.
- Сведение: порядок мержа (n8n → back → front), конфликты, финальные проверки,
  сводка пользователю (что готово, что мержим, чем проверено). Мерж пачкой —
  только после «ок» и НЕ во время live-прогона пользователя (спросить, если
  стенд может быть живым — деплой во время live-теста запрещён).
- **Очередь мержа (будка)**: внутри «пачки» ветки вливаются ПО ОДНОЙ через
  `hub_merge_join`/`hub_merge_leave` — мержится только голова очереди
  (`is_head: true`), каждая следующая ветка перед мержем ребейзится/сливается
  на СВЕЖУЮ основу. Так конфликты всплывают по одному и маленькими, а не кучей
  в конце. Готовые ветки, поменявшие одно место, отдельными worktree не
  лечатся — только очередью; в идеале пересечение ловится ещё на нарезке волн
  (MECE по файлам) и на бронях.
- После мержа: выждать CI и смоук predprod (/health + проверка фичи),
  тикеты → Готово с пруф-комментом (хэши, чем проверено).
- **Бранч-гигиена после мержа пачки — ОБЯЗАТЕЛЬНЫЙ шаг, не опция** (обкатка
  19.07: мержили честно, но без уборки скопились десятки мёртвых веток,
  worktree и claude/*-пустышек чип-сессий). Сразу после смоука диспетчер:
  1) по каждой ветке пачки и её worktree: `git cherry origin/main <ветка>` —
     все коммиты влиты (`cherry`=0 «+») → `worktree remove` + `branch -D` +
     `push origin --delete` БЕЗ вопросов (доказано влито, терять нечего);
  2) ветки с уникальными коммитами (`+` в cherry) — НЕ трогать: вердикт
     ЦЕННОЕ списком пользователю, удаление только по его «ок»;
  3) `claude/*`-ветки корня от чип-сессий: 0 уникальных коммитов → снести
     вместе с worktree (`.claude/worktrees/*`); `git worktree prune` везде;
  4) раз в пачку — быстрый аудит ВСЕХ репо (ahead/behind, worktree-привязки),
     чтобы чужие хвосты не копились молча.
- Когда ВСЕ тикеты эпика в Готово: собрать QA-тикет приёмки эпика по скиллу
  `qa-handoff` (assignee — QA-инженер). Эпик закрывается после закрытия QA-тикета.
- После рестарта приложения: некликнутые чипы пропадают — перевыдать (брифы
  целы в Jira); живые волны резюмить сообщением «продолжай волну по брифу».
