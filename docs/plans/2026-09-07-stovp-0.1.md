# План релиза stovp 0.1 — канон (плагин без пульта)

Дата: 07.09.2026. Статус: к исполнению в отдельной сессии. Решения CTO
зафиксированы в `docs/specs/2026-09-07-vibe-first.md` (модель) и
`docs/reviews/2026-09-07-plugin-audit.md` (вердикты по каждому куску).
Здесь — только порядок работ и приёмка. Кода в пульте (`morda/`) этот
релиз не трогает; `nyron-dev/` не правится ничем, кроме версии в
`marketplace.json`.

## Правила стройки

- Работать в этом репо в ветке от `main`; коммиты мелкие, заголовок
  `stovp: <что>`; пуш в `origin` (GitLab — канон), не в зеркало.
- Каждый шаг — проверка фактом (команда в колонке «приёмка»), не «сделано».
- Ничего не удалять из `nyron-dev/`: у команды он стоит и работает.
  Архив — `docs/archive/nyron-dev/` (копия того, что не переезжает).
- Астра-ревью на финале по `cross-review.sh -C . -b main -s L -e auto`.

## Шаги

| # | Что | Где | Приёмка |
|---|---|---|---|
| 0 | `marketplace.json`: версия `nyron-dev` = 0.14.2; вторая запись `stovp` 0.1.0, `source: ./stovp` | `.claude-plugin/marketplace.json` | `claude plugin validate .` зелёный |
| 1 | Каталог `stovp/` копией `nyron-dev/`; `plugin.json`: name `stovp`, version 0.1.0, описание в три слоя (рабочее место · узел · канон) | `stovp/.claude-plugin/plugin.json` | `claude --plugin-dir stovp` стартует |
| 2 | **Скилл `work`** — новый, ≤600 слов, структура: зачем → когда и когда НЕ → вход (чип `/goal` с пятью снятиями, или мелочь) → первый ход (worktree `wt/<слаг>`, стенд, зона правки) → ход без вопросов (реши, запиши, покажи) → финал (merge main → точечные тесты → Astra по `review.gate` → полный сьют → мерж сама → `worktree remove` → след по `trace` → сводка) → рационализации → red flags → verification. Уроки волн — таблица разбора §2.1, по строке | `stovp/skills/work/SKILL.md` | `python3 stovp/hub/skills-lint.py --quiet` |
| 3 | Убрать `vibe`, `nyron-waves` из `stovp/skills/` → `docs/archive/nyron-dev/skills/` | | lint |
| 4 | `cross-review`: секция «Встраивание в конвейер волны» → «Встраивание в work» (до мержа при `review.gate: behavior`, фоном для вёрстки); диспетчер → сессия; клички → роли карты моделей | `stovp/skills/cross-review/SKILL.md` | lint; скрипты не трогать |
| 5 | Карта моделей: дефолт `stovp/assets/models.yaml` (роли, харнессы, `can` — спека §2.6); читалка `stovp/hub/models.py <роль> <харнесс>` (машина `~/.stovp/models.yaml` → проект `models:` → личный → дефолт плагина); `engine.sh` берёт модель роли `review`, если `-m/-M` не заданы | `stovp/assets/`, `stovp/hub/models.py`, `stovp/skills/cross-review/scripts/engine.sh` | `python3 stovp/hub/models.py review codex` печатает `gpt-6-astra`; `cross-review.sh` без `-m` работает |
| 6 | `project-config`: файл проекта `.claude/stovp.md` (+`.local.md`); тело — протокол чтения + указатель на шаблон; шаблон: новые ключи `guard.prod`, `review.gate`, `work.port_base`, `trace`, `intake`, `qa`, `merge_rights: session\|human`; `models` — только отличия от карты; убрать `max_parallel_waves`, `models.dispatcher/wave` | `stovp/skills/project-config/` | lint; шаблон читается как YAML |
| 7 | `jira-task-standard`, `feature-intake`, `product-flow`, `intake-groom`, `team-roles`, `live-epic-testing`, `qa-handoff`: только ссылки (`nyron-waves` → `work`, «диспетчер» → «сессия», `nyron-dev.md` → `stovp.md`). `feature-intake`: механика прототипа → `references/prototype.md`, предохранители гриля → `references/grill.md` | `stovp/skills/*` | lint; `grep -rn nyron-waves stovp/skills` пусто |
| 8 | Хуки: `hooks.json` — SessionStart (политика + версия), **PreToolUse `guard.mjs`** (копия `morda/guard/pretooluse-guard.mjs` + правило 5 «данные прода» по `guard.prod` из конфига проекта; пустой список → предупреждение в stderr), Stop-хук снят. `code-policy.md` + шесть операционных правил (agent-skills `using-agent-skills` §Core Operating Behaviors, своими словами) | `stovp/hooks/` | тестовый вход `{"tool_name":"Bash","tool_input":{"command":"psql postgres://prod-host/db"}}` при `guard.prod: [prod-host]` → deny; `rm -rf ~` → deny; обычная команда → молчание |
| 9 | Будка: `server.mjs` без `hub_lock/unlock/merge_join/merge_leave` (8 тулов); в архив `hub-watch.sh`, `hub-rearm.sh`, `watch-nanny.sh`, `nanny-install.sh`, `watchdog*`, `wave-rules-sync.py`, `reuse-check.py`; `skills/nyron-waves/scripts/wave-tokens.py` вместе со скиллом; `wave-cleanup.sh` → `worktree-cleanup.sh` (тексты про волны → про рабочие сессии) | `stovp/hub/` | `bash stovp/hub/hub-ask-test.sh`, `hub-race-test.sh` (без секции локов), `hub-error-test.sh`, `hub-anchor-test.sh`, `transcript-test.sh` зелёные |
| 10 | `stovp/README.md` (три слоя, установка, конвенции), `CHANGELOG.md` запись `stovp 0.1.0`, `.claude/passport.json` репо → `stovp` и его скиллы | | `claude plugin validate .` |
| 11 | Финал: Astra-ревью диффа; один живой тикет psylia пройден `work` от чипа до мержа | | вердикт ПРИНЯТО; ветка влита сессией сама |

Не в этом релизе (0.2–0.4 по разбору §9): пульт (worktree на старте,
переписанный оркестратор-контекст, архив дежурного/автопинков/сторожа,
дерево «проект → сессии», `release.js` на два плагина, **`slotSync` с
личной памятью `memory/`** и раздача на Desktop-копии), скилл `onboard`,
doctor, Telegram.

## Промпт для новой сессии (вставить первым сообщением)

> Строим релиз stovp 0.1 по плану `docs/plans/2026-09-07-stovp-0.1.md`.
> Прочитай его, затем спеку `docs/specs/2026-09-07-vibe-first.md` и разбор
> `docs/reviews/2026-09-07-plugin-audit.md` (§1 сводка, §2 скиллы, §3 хуки,
> §4 будка). Решения CTO там зафиксированы — не переспрашивать. Работай по
> шагам плана, каждый шаг — коммит `stovp: …` и проверка фактом из колонки
> «приёмка». `nyron-dev/` не трогать (команда на нём работает), `morda/` не
> трогать (это 0.2). На финале — Astra-ревью и живой тикет.
