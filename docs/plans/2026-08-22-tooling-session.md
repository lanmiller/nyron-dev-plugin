# План сессии 22.08+: инструментальный контур до конца, потом хвосты флота

> **Ход 22.08 (вторая сессия):** Блок 1 закрыт целиком — цикл прогнан фактом
> (нашёл и вылечил project-pin 0.9.2, перекрывавший 0.10.0 во всём ai-evolve;
> копии дораздали, матрица теперь красит версии плагинов), подсказки /config
> переписаны + раскрывашка «как читать карточку», строгий MCP-профиль проверен
> живой сессией (t-strict видела ровно memory/slack/jira-nyron), кнопка выката
> собрана (release.js, панель на карточке nyron-dev). Из Блока 2 закрыты п.6
> (7 мёртвых карточек сняты, правило в триаже) п.7 (автосудья раз в 10 мин,
> вердикт в реестр и строку флота) и п.8 (судья-следователь: 4 read-only
> инструмента, проверен фактом — на пустом экране дёрнул все и отсудил верно).
> Осталось: п.9 толкач финала (STOVP-57), п.10 вижн-смок, Блок 3.

Приоритет постановщика: «хочу с плагинами, скиллами и коннекторами
разобраться — в первую очередь». Всё ниже — по фактам конца дня 22.08;
проверять заново, не верить этому файлу на слово.

## Блок 1 — инструменты: цикл «настроил → раздал → проверил» кнопками (ГЛАВНОЕ)

Фундамент есть: страница /config (матрица «инструмент × копия × проект»,
разряды аккаунтные/сессионные, канон morda/canon.json, гейт «основной сам
зелёный»), раздача slot_sync, ключница с показом значений, паспорт со
смоуками. Дожать до состояния «CTO прошёл цикл сам, без Claude»:

1. **Прогнать цикл фактом на пустой копии stovpe3tt** (она была голой:
   ни скиллов, ни superpowers, ни mcp_config). Раздать из /config, потом
   перезапуск служебной сессии слота, потом показать матрицей, что приехало.
   Расхождение = баг раздачи, чинить сразу.
2. **Наполнить canon.json по списку постановщика** (проверить, что все на
   месте и с верным разрядом): playwright, impeccable, shadcn-svelte,
   design-system, superpowers, memory MCP, find-docs, langfuse, плагин
   nyron-dev. Что фактически стоит, но вне канона — показать CTO списком
   «в канон или снести», решения вписать.
3. **Строгий MCP-профиль сессии**: раннер умеет strictMcpProfile
   (--mcp-config + --strict-mcp-config из паспорта + аккаунтные канона),
   но фактом на живой сессии НЕ проверено. Проверить: сессия на stovp
   видит только положенные серверы (claude mcp list изнутри). Решить с CTO,
   включать ли по умолчанию для bypass-сессий.
4. **Подсказки в /config**: словами объяснить разницу user-scope /
   project-scope / копии — CTO прямо говорил «я так и не понял, как
   работать с конфигами». Каждой кнопке — title, что именно она сделает.
5. **Выкат плагина кнопкой** (по решению CTO): сейчас цикл — бамп версии в
   двух json + CHANGELOG + push + `claude plugin marketplace update` +
   `claude plugin update nyron-dev@nyron-dev-marketplace-v2`. Собрать в
   одну ручку пульта с показом диффа версий; САМ выкат — только по кнопке
   человека, автоматом никогда.

## Блок 2 — хвосты флота (после блока 1)

6. **7 открытых автокарточек «Сессия молчит»** висят по мёртвым сессиям
   (nyron, 9+ ч). Расширить триаж: открытая автокарточка чек-ина, чья
   сессия мертва/запаркована — снимается сама. Прогнать, убедиться что
   счётчик карточек упал.
7. **Автовызов судьи при «похоже, встала»**: сейчас кнопка «разобраться»;
   сделать автоматический первый вердикт (код собирает улики → deepseek)
   с записью в карточку. Ключи JUDGE_* уже в ключнице stovp.
8. **Судья-следователь**: read-only тулы в judge.js (screen, tail, hooks,
   branches) через OpenAI tools-цикл — модель у CTO с поддержкой тулов
   (deepseek-v4-flash-vision-exp через OpenRouter).
9. **Толкач финала** (кусок STOVP-57): чек-ин ловит «закончила, ветка не
   влита» → судья судит → пинок сессии («влей по своим merge_rights или
   передай диспетчеру») или пост диспетчеру в будку. Мерж руками модели —
   НИКОГДА; только сессия/человек. Кнопки «влить»/«прибрать» в git-панели
   уже есть и проверены.
10. **Вижн-смок вёрстки**: скрин страницы пульта (playwright) → вердикт
    по UI от вижн-модели. Начать с одной страницы (/config), сравнить с
    находками impeccable.

## Блок 3 — мелочи, если останется место

- Баг: судья получил пустой экран tmux по живой сессии git (вердикт
  «экран пуст» при живом терминале) — проверить capture-pane имя/окно.
- pol должен был назвать счёт повторной критики против прежних 19/40 —
  доклад в ленте wt-polish сессии f7a8c40f; вытащить и записать в STATE.
- docs/tech-debt.md: разобрать первую запись (open_asks до stalledCard).
- Память сессий: записать урок «пробную ветку — только от чистого дерева»
  (коммит c66cba6 «проба» унёс кнопки мержа — сообщение врёт).

## Правила (те же, что всю неделю)

- Каждый шаг — фактом (вызов, скрин, вывод), не «должно работать».
- Работа — в ветке от main в отдельном worktree; ПРИБИРАТЬ кнопками
  git-панели («влить» → «прибрать»), они для этого и сделаны.
- Пульт на 4747 не перезапускать без пересборки bash morda/install-launchd.sh;
  dev-порт свой (5757+).
- Плагин nyron-dev: 0.10.0 выкачен; новые правки скиллов/хуков копить в
  ветке, выкат — только отдельным решением CTO (кнопкой из п.5, когда будет).
- UI — по дизайн-системе, новые куски сразу в app.css и на витрину /design.
- Ключи — только ключница; значения в контекст сессии не тянуть
  (keys_reveal — для человека в пульте).
- Тестовые сессии t-*, после теста парковать. Живой флот не трогать.

## Где что лежит

- Пульт: morda/ (runner.js, fleet.js, passport.js, judge.js, keys.js,
  configurator.js, git.js, checkin.js).
- Канон инструментов: morda/canon.json; паспорт проекта: .claude/passport.json.
- Спеки: docs/specs/2026-08-20-configurator.md, 2026-08-17-passport-v1.md.
- Тикеты: STOVP-59 (итог в комменте 12897), STOVP-57 (модель работы, в
  Ревью), STOVP-61 (гейт, влит), STOVP-63 (worktree как ветка — заведён).
