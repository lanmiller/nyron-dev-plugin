# Состояние на конец 22.08.2026 (снимок для передачи)

Всё ниже проверено фактом к 12:30 22.08. План следующей сессии —
`docs/plans/2026-08-22-tooling-session.md` (приоритет CTO: инструменты первыми).

## Закрыто и вылито

- **STOVP-59 закрыт по всем шагам**, итог — коммент 12897 в тикете.
  psylia: паспорт 13/13, аудитор прошёл целиком, MR влиты, деплои зелёные.
- **Плагин 0.10.0 выкачен и установлен** (проверено: фикс Stop-хука в кеше,
  скиллы 10/10). Правки дальше — копить, выкат отдельным решением CTO.
- **Пульт**: гейт паспорта на старт (STOVP-61), флот на главной, пульс CLI,
  детектор застревания (лента молчит 10 мин), очередь, живой терминал,
  git-панель с «влить»/«прибрать» (проверены фактом: отказ на невлитой),
  конфигуратор /config (матрица, разряды, канон morda/canon.json),
  ключница с показом значения по клику (хозяину — да, сессиям — забор).
- **Судья**: deepseek-v4-flash-vision-exp через OpenRouter, ключи JUDGE_* в
  .secrets/env stovp; 8 с на вердикт; кнопка «разобраться» у застрявшей.
  Автотриаж хвостов каждые 6 ч (первый прогон закрыл 13 мертвецов).
- Все ветки wt/* влиты и прибраны (локально и на origin), worktree снесены,
  проекты wt-* убраны из пульта. Сессии cfg/pol/mdl/plg/git запаркованы.

## Решения CTO, принятые сегодня (не переспрашивать)

- Судья — ВНЕ Claude-стека (прямой HTTP), «хайку бесит, что клод может
  зависнуть». Локальные модели в Docker — «жрёт много, непрактично».
- Значения ключей: хозяину в пульте видны по клику, сессиям закрыты.
- Модель судьи менять строкой JUDGE_MODEL (OpenRouter). У неё есть тулы и
  вижн — план: судья-следователь (read-only тулы) + вижн-смок вёрстки.
- Иерархия постановщик → диспетчер → волны жива (merge_rights в плагине);
  сегодняшние параллельные одиночки — не волны, их диспетчером был Claude.
- Приоритет следующей сессии: плагины/скиллы/коннекторы — «не понимаю
  логику»; причина найдена: карточка /config не объясняет разницу
  «стоит» (факт с диска) и «требуется» (паспорт). Переписать подсказки.

## Хвосты — состояние после второй сессии 22.08

- ~~7 автокарточек «Сессия молчит»~~ — сняты; правило в триаже (judge.js).
- ~~Автовызов судьи~~ — автосудья раз в 10 мин (hooks.server.js).
- ~~Пустой экран у судьи~~ — ошибка tmux теперь сама улика, не пустота;
  плюс судья-следователь дозапрашивает read-only инструментами.
- **pol: счёт повторной критики — 35/40 против прежних 19/40** (лента
  f7a8c40f; P0/P1 не осталось; хвост P2–P3 и три вопроса CTO — в снапшоте
  `.impeccable/critique/2026-08-21T15-08-33Z__morda-src-routes-s-project-key-page-svelte.md`).
- ~~docs/tech-debt.md~~ — запись open_asks/stalledCard закрыта кодом (fleet.js).
- Урок «пробную ветку — только от чистого дерева» — записан в память проекта.
- STOVP-63 заведён (worktree как ветка проекта в пульте, кнопка «прибрать»
  уже есть — остальное там).

## Живое

- Пульт 4747 (launchd), туннель tuna — если жив процесс. Docker: 10 CPU /
  33 ГБ, модели НЕ скачаны (CTO качает сам из GUI при желании).
- Канон ai-evolve (35 КБ, шаг 3 для него) — не тронут, отдельное решение.
