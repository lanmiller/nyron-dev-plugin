# Перечень долга

Строки из кросс-ревью с меткой [ДОЛГ]: ничего не ломают, разбираются в
свободные слоты (SYSTEM-DESIGN §8). Формат: дата · источник · суть · где.

(пусто — единственная запись про open_asks/stalledCard закрыта 22.08:
свежая карточка досчитывается в счётчик того же опроса, fleet.js)

- 23.08 (диспетчер STOVP-64): раннер `runnerStart` → `sendLine` ставит `goalSent: true`, но при старте сессии `w1-speed` Enter потерялся (цель осталась в строке ввода, CLI ещё грузился) — дожато `action: key Enter` руками. Нужна проверка доставки цели по экрану (строка ввода пуста / лента началась), иначе «мёртвый запуск» выглядит живым. Кандидат в STOVP-69 или отдельный тикет.
- 23.08 (диспетчер STOVP-64): Context7 MCP поднимает elicitation-диалог «войти для лимитов» прямо в сессии волны — bypass-сессия встала на нём (w1-speed, 15:25Z), снято Esc через раннер. Детектор застревания/tui.js должен распознавать MCP-elicitation как диалог (Accept/Decline) и либо авто-Decline, либо карточку в будку; плюс `npx ctx7 setup` один раз на машине снимает причину.
- 25.08 (оркестратор, разбор «ЖДУТ ВАС» с CTO): карточки надзирателя «watchdog-inferred» бесполезны человеку — в question попадает заголовок сессии или огрызок, в context — одна и та же заглушка. Надзиратель уже читает хвост транскрипта — пусть кладёт в карточку САМ вопрос ассистента (последний вопросительный абзац) и пару строк вокруг. Где: nyron-dev/hub/watchdog.mjs (генерация ask), morda AskCard — отображение.
- 25.08 (оркестратор, тот же разбор): карточка «Сессия молчит: посмотреть или перезапустить?» спрашивает про действия, которых не предлагает — единственная кнопка «вижу, разбираюсь» ничего не делает. Либо реальные кнопки (перезапустить / экран / судья) через раннер, либо не показывать человеку вовсе (тишина и так видна в списке флота, судья+триаж работают сами). Где: morda/src/lib/server/checkin.js:123 (stalledCard), AskCard.svelte.
- ~~25.08 (оркестратор): постановщику в Desktop не видна очередь вопросов будки~~ — закрыто 25.08 (0c507d8): pult_asks + pult_answer (answer/clarify/cancel) в pult-mcp, канон «до человека доходят только понятные вопросы» в instructions. Осталось из того же разбора: карточки надзирателя (частично закрыто 0c507d8 — question+gist из транскрипта; старые заглушки не переписываются) и кнопки у «Сессия молчит» (открыто, checkin.js:123).
