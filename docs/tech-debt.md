# Перечень долга

Строки из кросс-ревью с меткой [ДОЛГ]: ничего не ломают, разбираются в
свободные слоты (SYSTEM-DESIGN §8). Формат: дата · источник · суть · где.

- 2026-08-22 · cross-review STOVP-60 r4 · счётчик `open_asks` в списке
  сессий собирается до создания карточки «застряла», поэтому на первом
  опросе после перехода бейдж карточки ещё 0 и догоняет на следующем
  (секунды) · morda/src/lib/server/fleet.js (open до stalledCard).
