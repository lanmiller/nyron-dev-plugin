# Перечень долга

Строки из кросс-ревью с меткой [ДОЛГ]: ничего не ломают, разбираются в
свободные слоты (SYSTEM-DESIGN §8). Формат: дата · источник · суть · где.

(пусто — единственная запись про open_asks/stalledCard закрыта 22.08:
свежая карточка досчитывается в счётчик того же опроса, fleet.js)

- 23.08 (диспетчер STOVP-64): раннер `runnerStart` → `sendLine` ставит `goalSent: true`, но при старте сессии `w1-speed` Enter потерялся (цель осталась в строке ввода, CLI ещё грузился) — дожато `action: key Enter` руками. Нужна проверка доставки цели по экрану (строка ввода пуста / лента началась), иначе «мёртвый запуск» выглядит живым. Кандидат в STOVP-69 или отдельный тикет.
- 23.08 (волна 1, STOVP-65, критика находок + Sol): первый `list()` индекса после старта пульта — холодный разбор всех лент проекта синхронно (2,0–2,2 с на 2830 сессиях; на main было столько же). Прогрев индекса вне запроса (init-хук SvelteKit или ленивый разбор заголовков вне окна 48 ч) — `nyron-dev/hub/transcript.mjs: createIndex`.
- 23.08 (волна 1, STOVP-65, критика): наблюдатели каталога слага, созданные через `activityOf` (путь чек-ина, `checkin.js: lastActivityMs`), живут до конца процесса — владельца-подписчика у них нет; число ограничено числом каталогов слага (~150 на машине). Нужен владелец в `checkin.js` или TTL — `transcript.mjs: observeSlug`.
- 23.08 (волна 1, STOVP-65, критика): `nyron-dev/hub/transcript.mjs` вырос до ~990 строк и несёт три темы (разбор лент, наблюдение ФС, индекс). Разнести в модули, не меняя места экспорта (зафиксировано тестами и `watchdog.mjs`).
- 23.08 (волна 1, STOVP-65, фиксер r2): `sync()` при СОБЫТИИ о составе каталога слага перечитывает каталог одним куском в пути запроса (событийный путь, не слепой); на проекте с тысячами лент это десятки мс на редкое событие «новый/удалённый файл» — `transcript.mjs: createIndex.rescan`.
- 23.08 (волна 1, STOVP-65, критика): тест DoD-3 «повторный list без изменений — 0 fs» не может провалиться по построению (два вызова подряд внутри окна ресинка 10 с); живой пруф снят скриптом (10 опросов: main 84 757 fs-вызовов → ветка 40), тестом не зафиксирован — `morda/tests/session-index.test.mjs`.
- 23.08 (волна 1, STOVP-65, кодер): рекурсивный `fs.watch` на Linux — inotify на каждый подкаталог; на тысячах каталогов сессий возможен упор в `fs.inotify.max_user_watches` (деградация мягкая: вотчер не поднялся → фоновый обход раз в 10 с). Для линуксовой машины пульта проверить лимит.
