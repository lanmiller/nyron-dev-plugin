# Патч конфига ai-evolve: реестр внешних участников, первая запись — Марина

Решение постановщика 22.08.2026 (STOVP-51, дыра «люди без плагина»).
Забор раннера не пустил сессию wt/plugin к файлу вне её корня — блок ниже
вставить руками (или сессией ai-evolve) в `/Users/stovp/ai-evolve/.claude/nyron-dev.md`,
в конец секции `roles:` (после записи `qa:`), внутри фронтматтера:

```yaml
  # ── Реестр внешних участников (решение постановщика 22.08.2026) ──
  # У кого plugin: false — НЕТ будки, hub_post, скиллов и MCP плагина.
  # Скилл, составляющий промт человеку, ОБЯЗАН свериться с этим реестром:
  # участнику без плагина — никаких hub_post и скиллов в инструкциях,
  # отчёт — по report_channel (чат/коммит вместо будки).
  marina:
    plugin: false              # работает из своего Клода, плагина nyron-dev нет
    report_channel: "чат и коммит документов"
    notes: "Не разработчик и не продакт: выполняет задачи и коммитит документы."
```

Формат канонизирован в `nyron-dev/skills/project-config/assets/config-template.md`
(секция roles) — после применения патч-файл удалить.
