# HANDOFF — состояние на 07.09.2026

Точка входа для новой сессии. Прежний хендофф (этап 4 морды, 09.08) —
`docs/archive/HANDOFF-2026-08-09.md`.

## Что решено 07.09 (CTO)

Плагин переосмыслен: рабочая сессия вместо флота волн, вайб — дефолт,
сессии доезжают сами, ветка и worktree на каждую сессию, мерж — сессия
сама при зелёной Astra и CI, пульт — удалённое управление и узел (ключница,
паспорт, слоты, конфиги), карта моделей — один файл на машине. Новый плагин и репо
`vibe-pult` (имя — решение CTO 07.09; STOVP остаётся именем программы), `nyron-dev` заморожен на 0.14.2 без правок — на нём работает вся
команда с личными конфигами. **Новый репо — `lanmiller/vibe-pult` на GitHub,
приватный, GitHub — канон; заводит CTO руками** (интеграция сессий не имеет
права создавать репозитории). Плагин едет туда сразу, пульт — релизом 0.2.

Читать в порядке:

1. `docs/specs/2026-09-07-vibe-first.md` — модель: рабочая сессия `work`,
   пять снятий на планировании, чип `/goal`, две границы, пульт, карта
   моделей (§2.6).
2. `docs/reviews/2026-09-07-plugin-audit.md` — разбор от и до: вердикт по
   каждому скиллу, хуку, файлу будки и модулю пульта; подключение проекта
   как парадная дверь (§6); рынок и тезис узла (§11); план релизов (§9).
3. `docs/plans/2026-09-07-vibe-pult-0.1.md` — что строить первым, по шагам, с
   приёмкой и промптом для сессии.

## Промпт восстановления

> Строим релиз vibe-pult 0.1. Репо — `lanmiller/vibe-pult` (GitHub, канон); исходник
> и доки — `lanmiller/nyron-dev-plugin`, ветка
> `claude/plugin-agent-skills-0l0vg0`. Подключи оба, прочитай
> `docs/plans/2026-09-07-vibe-pult-0.1.md` целиком, затем спеку и разбор из
> HANDOFF.md. Решения CTO зафиксированы — не переспрашивать. Начни с seed
> нового репо (шаг 1). `nyron-dev/` и `morda/` не трогать.

## Каноны, которые легко забыть

- Плагин-репо: канон — GitLab `origin`; GitHub — push-зеркало с выключенным
  keep_divergent_refs, прямой пуш туда живёт до следующего синка.
- Бамп версии — в двух манифестах одним коммитом (`plugin.json` +
  `marketplace.json`); `marketplace.json` сейчас отстал (0.11.0 ↔ 0.14.2).
- Ревью — крест-накрест по автору: `cross-review.sh -e auto`, фоном.
- Тесты будки: `bash nyron-dev/hub/{hub-ask,hub-race,hub-error,hub-anchor,transcript}-test.sh`.
