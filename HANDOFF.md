# Хендофф: состояние работ по плагину и STOVP

Точка входа для новой сессии. Обновлено: 05.08.2026.
Читать в этом порядке: [VISION.md](VISION.md) — зачем и куда;
[SYSTEM-MAP.md](SYSTEM-MAP.md) — 25 функций «сейчас → будет»;
этот файл — что уже сделано и что делать дальше.
Идёт пилот общего флоу (с 03.08) — находки процесса и их разбор:
`ai-evolve-docs-test/PILOT.md` (журнал в docs-репо — он есть у всех
участников; их Claude распознаёт и пишет находки сам, люди только кивают).

**Рамка сессии**: работаем над плагином и системой STOVP. Продуктовые дефекты
проектов (evolve, psylia и прочие) в этой работе не разбираем — для них свои
эпики и волны.

## СОСТОЯНИЕ НА 05.08.2026 — пилот общего флоу пройден, команда внутри

Три дня (03–05.08) шёл пилот: гриль-постановка фичи, контуры не-код ролей,
онбординг живых людей. Итог — **плагин 0.8.0 → 0.8.15 (16 релизов),
22 находки в журнале, почти все закрыты механизмами в тот же день**.

### Плагин: что нового в 0.8.x (всё в feature-intake, если не сказано иное)

| Версия | Правило |
|---|---|
| 0.8.0 | гриль-механика Р1 (покрутить → гриль с рекомендованным ответом), прототип из ДС, предохранители от «да-да-да»; синхронизирован шаблон тикета |
| 0.8.1 | показ прототипа во встроенном браузере; файл-требование пишется РАНО и дописывается по ходу |
| 0.8.2–0.8.3 | десктопный вьюпорт предпросмотра; путь прототипа строго `/design-system/proto/<слаг>` |
| 0.8.4 | ролевые линзы (продакт + UX) и проверка посылов (ux-copy) до финала |
| 0.8.5 | якорь в каталоге путей; живой пруф вместо git-археологии; самопроверка визуала до показа |
| 0.8.6 | «дома общего элемента» — все места использования по коду до вопросов |
| 0.8.7–0.8.8 | переключатель «чистый вид»; вариативность дизайна (A/B/C + полу-гриль) |
| 0.8.9–0.8.10 | самодостаточность требования (скриншоты + раздел «Дизайн» + смоук читателем); блок «Дизайн» в тикетах |
| 0.8.11–0.8.12 | скоуп-стоп («Идеи на потом»); свежесть сессии (выгорела → передача через файл) |
| 0.8.13–0.8.15 | сквозной живой прогон + запрет самодельных копий; судьба прото-ветки (общее в main сразу, ветка в свежести); **прототип мержится в main** (раздел «Прототипы» витрины, прод закрыт флагом) |

### Команда вошла в флоу

- **Продакт (Юра)**: окружение поднято, плагин 0.8.15, Jira работает.
  Нагрилил 4 требования (internal-admins, модерация хаба, обложки,
  публичный профиль автора), 3 прото-ветки. В работе: мерж требования,
  вынос общей правки витрины в main, мерж прототипов, догруминг
  DEV-961/944/943.
- **QA (Агиль)**: ставится, Обсидиан-хранилище = корень `~/nyron`, право
  писать в main docs-репо выдано; завёл ветку находок пилота.
- **Разработчик (Денис)**: предупреждён, промт «вкурить новый флоу» готов.
- **Методолог (Иван)**: без изменений, работает в
  `front/src/lib/artifact-components` по своему канону (ветка + MR).
- **Дизайнер**: контур готов (`ui/CLAUDE.md`), ждёт подключения.

### Права в GitLab (роздано через API, зафиксировано в TEAM-SETUP)

| | docs-test | front | корень | плагин |
|---|---|---|---|---|
| Продакт | пишет main | ветки + MR | пишет main | читает |
| QA | пишет main | читает | пишет main | читает |
| Методолог | читает | ветки + MR | читает | читает |
| Денис | пишет | **мержит main** | пишет | пишет |

`main` фронта закрыт до Maintainer (мержат CTO и Денис); docs-репо и корень
открыты Developer'ам (иначе канон журнала «пиши находку сразу» невыполним).

### Инфраструктура онбординга (в docs-репо)

- `TEAM-SETUP.md` — установка одним промтом (клоны + плагин через CLI +
  проверка файлов-маркеров), Обсидиан, подключения, права.
- `PILOT.md` — журнал находок пилота; Claude участника пишет туда сам.
- `.mcp.json.example` (корень) — шаблон подключений без секретов.

### Хвосты (кнопки CTO)

1. **GitHub-зеркало плагина → private** — сейчас публично торчит вся
   операционка (VISION, SYSTEM-MAP, хендофф, скиллы).
2. Принять MR перетряски дизайн-системы и хвосты чипа (DEV-945/964).
3. Прогон №4 гриля («единый умный чат-инпут») — экзамен флоу на чистой
   витрине; после него развернуть первое требование в эпик.
4. Разбор пилота: 22 находки в журнале ждут исходов (правка/канон/отвод).

## Что решено (кратко; обоснования — в VISION)

1. **Строим STOVP** — оркестратор фабрики: демон `stovpd` (Python/FastAPI),
   веб-морда Stovp (SvelteKit) + Telegram Mini App, CLI. Плагин nyron-dev
   остаётся боевым всю стройку; big-bang запрещён.
2. **Job-модель** — единица исполнения: взял задание → шаг → чекпойнт →
   завершился. Вотчеры и пробуждения уходят как класс.
3. **Порядок дорожной карты**: 0 гигиена → **2 UJM и каноны** → 1 сигнал
   (алерты, бэкап, безопасность) → 3 демон → 4а/4б флот → 5 продуктовая
   петля. UJM переехал вперёд: каталог путей — линейка серьёзности для всей
   фабрики и формат передачи работы QA.
4. **Триаж находок тремя корзинами** (в канон волн): ломает путь из
   каталога → дефект; поведения в каталоге нет → вопрос замысла
   постановщику; не ломает → перечень долга. Плюс потолок кругов ревью
   (2, для сложного 3) — дальше решение человека, а не следующий круг.
5. **Стандарт задачи усилен**: обязательное поле «путь UJM / шаг»,
   «Как тестировать» разделено на «Приёмка руками» и «Проверки
   разработчика», гейт статуса (нет приёмки — нет перехода в тестирование).
6. **Документация тремя слоями**: репо/vault — истина, Confluence — витрина
   (рендер md2conf), внешний тест-менеджмент — проекция каталога UJM.
7. **Секреты — три хранилища по потребителю**: переменные CI проекта/группы
   для пайплайнов, локальный стор машины для демона и ботов, ssh-config для
   подключений. В документах — имена, не значения.
8. **Пакеты плагина по ролям** (потом, этап 2): core / product / qa /
   dispatch; роль = пакет, проект = конфиг.

## Что сделано за 27.07–01.08

**Плагин, версии 0.7.8–0.7.9** (в GitLab, кэш маркетплейса обновлён):
- вотчер: перевзвод стал заменой (дубли невозможны), реаниматор-одиночка,
  чтения будки с busy-timeout, ошибка чтения ≠ ноль;
- канон ожидания v2.1 + запрет параметра timeout на вотчерах и ревьюере;
- handoff-канон: переезд задачи между аккаунтами чекпойнт-файлом;
- шаблон конфига: шесть ключей статусов вместо четырёх;
- `hub/jira-slim.py` — прокси-фильтр эха трекер-сервера (сжатие ×246 на
  статусных операциях), подключён в двух проектах.

**Документы** (в этом репозитории): VISION, SYSTEM-MAP, четыре ресёрч-
дайджеста, план имплементации от внешней модели.

**Канон в docs-репо проекта**: правила волн v2.1; каталог путей `ujm/` —
формат-эталон (карта пути + прогон 15 минут + таблица покрытия).

## Задачи — в Jira, проект STOVP (приватный, доступ только у CTO)

Роадмап переехал из этого файла в трекер 06.08.2026. **Единственный дом
задач стройки — проект STOVP**; HANDOFF остаётся статус-документом и точкой
входа сессии, бэклогом он больше не является.

| Эпик | Что внутри |
|---|---|
| [STOVP-1](https://nyron.atlassian.net/browse/STOVP-1) | Плагин nyron-dev: правки скиллов, баги, релизы |
| [STOVP-2](https://nyron.atlassian.net/browse/STOVP-2) | Этап 0 — гигиена фабрики (8 задач) |
| [STOVP-3](https://nyron.atlassian.net/browse/STOVP-3) | Этап 1 — сигнал (5 задач) |
| [STOVP-4](https://nyron.atlassian.net/browse/STOVP-4) | Этап 2 — каноны и каталог путей (14 задач) |
| [STOVP-5](https://nyron.atlassian.net/browse/STOVP-5) | Процесс команды: онбординг, права, флоу (3 задачи) |

Порядок этапов и обоснования — в [VISION.md](VISION.md); карта функций —
[SYSTEM-MAP.md](SYSTEM-MAP.md). Размер задачи (S/M/L) — префиксом в заголовке.

**Сделано пилотом 03–05.08, тикетов не заводилось**: гриль-механика
feature-intake (16 релизов 0.8.x, см. CHANGELOG), MVP контуров не-код ролей
(HOME-файлы, Obsidian-конфиг, канон дизайн-системы), онбординг команды,
права репозиториев. Хвосты этих работ — задачами в STOVP-4 и STOVP-5.

**Входящие**: жалобы, повторные вопросы и находки процесса пишет Claude
любого участника в `ai-evolve-docs-test/PILOT.md` (без спроса, повторы
помечаются ×N). Разбор раз в неделю у CTO → тикеты STOVP.

## Кнопки постановщика (без них задачи не стартуют)

1. ~~Обновить плагин~~ СДЕЛАНО 01.08 (0.7.9; 03.08 собран 0.8.0 — обновить ещё раз).
1а. **Запуск команды в общий флоу (MVP 0.8.0 → 0.8.13, пилот 03–04.08):**
   доступы GitLab розданы всем (04.08, API); TEAM-SETUP с промтом установки
   в docs-репо; CLAUDE.md дизайн-системы закоммичен чипом перетряски
   (MR !109), витрина перетряхнута (три раздела: актуальное / TODO-дизайн /
   прототипы; DEV-945..952). Осталось: полдня продакта на первый гриль;
   задача продакту — каталог путей (2.1); **GitHub-зеркало плагина →
   private** (находка безопасности 04.08 — публично торчит вся операционка).
2. Создать трекер-проект STOVP.
3. Занять имена: организация на хостинге кода, домен, поддомен туннеля.
4. Оплатить подписку дешёвой QA-модели (нужна для задачи 2.4).
5. Ответить на вопрос 2.2 после разговора с QA-инженером.

## Правила работы с этими документами

- Решение принято — оно попадает в VISION (принцип или раздел) или в
  SYSTEM-MAP (функция), а не остаётся в переписке.
- Задача сделана — строка отсюда уходит, в CHANGELOG появляется запись.
- Продуктовые дефекты проектов сюда не попадают: у них свои эпики.
