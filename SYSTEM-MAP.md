# Карта системы: что есть → что будет (по функциям)

Дата: 2026-07-27. Пара к [VISION.md](VISION.md): VISION — зачем и куда,
эта карта — предметно по каждой функции. Обновляется по мере строек.

Строим волнами нашего же плагина. Колонка «Этап» — привязка к дорожной
карте VISION (0 гигиена / 1 вышка / 2 UJM / 3 демон / 4 флот / 5 петля).

## Сводная таблица функций

| # | Функция | Сейчас | Будет | Чем строим | Этап |
|---|---|---|---|---|---|
| 1 | Постановка и груминг задач | Jira + скиллы плагина (jira-task-standard, intake-groom, feature-intake, product-flow); работает | + **вход для членов команды-постановщиков** (образец — Юра, nyron): собирает мокап экранов на дизайн-системе + фич-реквест в docs-репо/Jira (работает как Агиль: плагин + docs-репо + Jira, в чужие код-зоны не ходит) → intake-groom причёсывает → диспетчер грумит эпик. Паспорта тикетов — машинно-читаемые для job'ов | своё (есть, дописать вход в feature-intake) | — |
| 2 | Запуск исполнителей | человек кликает чипы в 4–5 Parall-клонах Claude Desktop (не виртуалки), модель — пикером | демон ставит job в очередь → headless `claude -p`/`--resume` ЛОКАЛЬНО по профилям `CLAUDE_CONFIG_DIR` (та же изоляция, что у Parall-клонов); модель — параметром; VM — опция усиления на будущее | своё ядро + пилот Claudexor (профили/квоты/ротация) | 4а–4б |
| 3 | Коммуникация агентов (будка) | SQLite+MCP: сообщения, брони, очередь мержа; вотчеры-скрипты в сессиях (болеют — диагноз 27.07) | демон владеет базой; схема усилена: jobs/attempts/leases/events, идемпотентность, FTS5, git-аудит; вотчеры исчезают (job-модель) | своё + паттерны MCP Agent Mail | 0→3 |
| 4 | Наблюдение человека | глаза в 4 окна + sqlite-запросы руками; о сбоях узнаёт человек раньше системы | веб-вышка Stovp (SvelteKit, localhost, Tailscale для удалёнки): доска волн, лента будки, очередь, окно-транскрипт любого исполнителя (поле ввода = создать control-job-продолжение через `--resume`, не живой чат); Telegram-алерты; телефон — да | своё тонкое + сенсоры готовые (OTel Claude Code, ccusage, oauth/usage) | 1 |
| 5 | Лимиты и экономика | ручная парковка по канону; 4 подписки насыщены; учёта нет | демон: живые квоты per-аккаунт (oauth/usage + ccusage), ротация по типизированным limit-сигналам, каскад, парковка = «не спавним job'ы»; API — переливной клапан с отдельной строкой расхода на вышке | ccusage + паттерны Claudexor/auto-resume + своё | 1 (видимость) → 4 (управление) |
| 6 | Кодовая работа | Workflow-конвейер тикета (тестген→импл→адверсарка→cross-review), TDD (тесты пишет Sol, код их не трогает), лестница+quality_floor, Context7, RAG codebase-memory | то же самое ВНУТРИ job'ов; изоляция тестов от кода становится физической (разные job'ы, дифф-гейт); переиндексация RAG — фоновый job по событию мержа | своё (есть), перенос | 3–4 |
| 7 | Ревью и челлендж планов | пара: Claude пишет — GPT (Sol/Terra) проверяет; план-челлендж обязателен | + третий голос (GLM) консилиумом на сложных решениях; обычные тикеты — парой как сейчас | codex CLI (есть) + GLM-подписка | 2 |
| 8 | QA продукта | самодельные смоуки волн, разрозненные e2e; ложные вердикты «с чужого зеркала» | библиотека UJM (playwright-bdd: .feature-каталог, владелец Агиль) + Test Agents (Healer чинит в MR) + ночная регрессия GLM + reports-server (флаки, тренды) | готовый стек + ~50 строк мультиюзер-фикстур | 2 |
| 9 | Рефакторинг (см. раздел ниже) | список ~100 задач, делается вручную по случаю; легаси от разных моделей | автономный фоновый контур: сканеры долга → скоринг → гейт покрытия → атомарные job'ы → консилиум-ревью → кап MR/день | своё поверх job-модели + knip/граф/UJM | 2 (гейт) → 4 (фон) |
| 10 | Мерж и деплой | самодельная очередь в будке (призраки — диагноз 27.07); мержит диспетчер | GitLab MR/merge trains — источник истины; в будке только локальные брони worktree; CI/деплой без изменений | GitLab (есть) | 0 (чистка) → 3 |
| 11 | Здоровье исполнителей | реаниматор-скрипт (зомби), руки; Desktop-копии жрут CPU в простое — лечится рестартом руками | демон-supervision: heartbeat, CPU-вотчдог (алерт → авто-рестарт), протухшие lease подбираются штатно | паттерны agent_farm + своё | 0 (алерт-скрипт) → 3 |
| 12 | Знания и документация | docs-репо + Confluence руками; в betzo всё на vault+Confluence; дизайн-системы: psylia есть, evolve строится, betzo казиношная (выделить — бэклог), noproblem прояснить | **три слоя** («репо — как устроено, vault — что думаем, Confluence — что показываем»): (1) репо/vault — source of truth (vault-канон по betzo-модели: пакет-папки гипотез, канон-файлы с хозяином и сроком годности фактов, тип в имени файла, Bases-доски); (2) Confluence — витрина, рендерится CI-пайплайном **md2conf** (поддерево «Docs (auto)», привязка по page-id, баннер generated, правит только CI-юзер; план Б — mark). Документирует та роль, что меняла — правкой источника. QA-человек (Агиль) работает плагином поверх docs-репо + Jira, БЕЗ доступа к код-репо — норма и модель для всех не-код ролей | md2conf в GitLab CI + betzo-vault-канон + obsidian-mcp | 2+ |
| 15 | Самонастройка репозитория | подключение нового репо к системе — руками (CLAUDE.md, .mcp.json, конфиг плагина пишутся по памяти) | скилл/команда плагина «init-repo»: сам ставит CLAUDE.md по канону, .mcp.json, .claude/nyron-dev.md, проверяет MCP-смоуком; сессия при старте валидирует конфиг и чинит дрейф | своё (скилл в плагине) | 0–2 |
| 16 | Канон CLAUDE.md (мультирепо) | пишутся стихийно; в evolve многорепо с иерархией, в betzo — продуктовая работа + мини-репы с кодом; дрейф и дубли | стандарт в плагине: корневой CLAUDE.md = карта + ссылки, пер-репо = только своё, лимит размера, обязательные блоки (окружения, запреты, тесты); линт-проверка — сначала ручным прогоном, фоновым job'ом после демона | своё (reference в плагине) | 2 (правила) → 3 (фон) |
| 17 | Трекер-канон per-project | evolve: DEV-доска, работает; betzo: ОДНА доска — продуктовые и рабочие задачи смешиваются; psylia: модель реконструирована (дайджест №2) — 6-ступенчатая лестница через ремап конфига, маршруты KAN(dev)+PROD(product), слой STATUS.md+ROADMAP.md над трекером; noproblem: ? | канон из psylia-модели: `statuses:` 6 ключей (в шаблоне сейчас 4 — баг, фикс в этап 0), `routes:` dev+product (product-тикет = родитель/гейт dev), STATUS-журнал + ROADMAP-потоки («в Jira только приоритизированное»), категории Jira-статусов — часть канона | своё (project-config + jira-task-standard) | 0 (фикс шаблона) → 2 |
| 19 | Дистрибуция плагина по ролям | один плагин nyron-dev, фул-набор у всех (Юре/Агилю/Марине не нужны волны и диспетчерство) | маркетплейс STOVP с пакетами: `stovp-core` (всем: стандарты, конфиг, будка) + `stovp-product` (Юра/Марина) + `stovp-qa` (Агиль) + `stovp-dispatch` (диспетчер); переименование скиллов под STOVP тем же рефактором, одна миграция | своё (разбивка плагина) | 2 |
| 18 | Git-гигиена | правило бранч-гигиены есть в wave-protocol, но руками диспетчера → копятся мёртвые ветки в origin и worktree/локальный мусор («пиздец» подтверждён обкаткой) | фоновый job-уборщик: `git cherry` — доказать влитость → снести ветку+worktree+origin-ветку без вопросов; уникальные коммиты → список человеку на вышку; TTL на worktree; метрика «мусора осталось» на вышке | своё поверх job-модели (правила уже написаны в wave-protocol) | 0 (разовая уборка) → 3 (фон) |
| 13 | Продуктовая петля | скиллы есть (гипотезы, замеры), замыкание руками | **гипотеза = родительская карточка** (флоу betzo): при утверждении в неё сразу заводятся и линкуются dev- и design-таски — прогресс трекается сквозь карточку гипотезы; телеметрия (PostHog, Langfuse, UJM-прогоны) автоматически становится замером → вердикт → следующая гипотеза | своё поверх Langfuse/PostHog (дописать правило линковки в product-flow) | 5 |
| 14 | Не-кодовая работа (ресёрч betzo, аналитика PostHog, контент psylia/noproblem) | волны уже делают (KAN-68/69: сотни статей), но страдают от вотчеров | те же job'ы: тип работы определяет адаптеры (PostHog API, CMS/контент-репо) и верификатор (кросс-челлендж, редполиси, sanity-чек цифр) — механика одна | своё (job-модель) | 4 |

## Рефакторинг-контур (функция 9, развёрнуто)

Проблема: ~100 задач рефакторинга; легаси написано разными моделями в разные
эпохи — «чёрт ногу сломит»; руками это не разгрести, а бросить нельзя
(ровный код = скорость всех будущих волн).

Принцип безопасности (из dev-best-practices, кейс «эталонного верификатора»):
**арбитр рефакторинга — существующие тесты и UJM, не мнение модели.**
Рефакторинг = «поведение не изменилось»: сьют и затронутые UJM зелёные до и
после. Где проверки нет — рефакторить запрещено, сначала появляется проверка.

Конвейер (все шаги — job'ы, человек только на вердиктах):

1. **Инвентаризация — не руками.** Фоновые сканеры пополняют живой реестр
   долга: мёртвый код (knip/vulture), дубли механизмов (граф
   codebase-memory), файлы-переростки (CODE_SIZE_GUIDELINES), устаревшие
   паттерны от старых моделей. Существующий список 100 задач загружается
   в реестр как затравка — и проходит дедуп против сканеров.
2. **Скоринг: рефакторим там, где ходим.** Приоритет = горячесть файла
   (частота правок волнами по git-истории) × размер долга. Холодное легаси,
   куда никто не лезет, лежит в хвосте — трогать его дороже, чем терпеть.
3. **Гейт покрытия.** Job разрешён только там, где участок накрыт тестами
   или UJM. Нет покрытия → сначала job «характеризационные тесты»
   (зафиксировать ТЕКУЩЕЕ поведение как есть, даже кривое) — и только
   потом рефакторинг.
4. **Исполнение атомарно.** Один механизм = один job = один маленький MR
   (дешёвая модель — GLM/Sonnet; лестница и quality_floor охраняют от
   «улучшайзинга»: цель — меньше сущностей, не «красивее»). Легаси-политика
   плагина действует: дубль → снос по правилу трёх исходов.
5. **Верификация.** Полный сьют + затронутые UJM + кросс-ревью другой
   семьёй; дифф-гейт «тестовые файлы не изменились» (кроме шага 3).
6. **Фоновый режим с капом.** Как ночная регрессия: простаивает квота →
   конвейер берёт следующий пункт реестра. Кап N MR/день — чтобы ревью и
   очередь мержа не захлебнулись. Реестр на вышке: сколько долга, скорость
   сгорания.

Итог: 100 задач превращаются из мёртвого списка в самопополняющийся реестр,
который фабрика сжигает фоном на дешёвых токенах, с математической страховкой
«поведение не изменилось».

## Что осознанно НЕ строим

- Замену Jira/будки новой шиной — дорабатываем существующие.
- GUI-автоматизацию Desktop-окон (мышкой по скриншотам) — окна для человека.
- Свой дашборд метрик с нуля — сенсоры готовые (OTel/ccusage), своя только
  доска волн над нашей же SQLite.
- Свой merge-механизм — GitLab merge trains.
- Форк Claudexor сейчас — зависимость + PR-политика; форк по триггеру
  (проект умер / PR-путь заглох).
