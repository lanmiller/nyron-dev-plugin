# VISION: автономная система разработки продуктов

Дата: 2026-07-27. Статус: рамка, согласована с постановщиком. Не спека.
Перед любой стройкой — план-челлендж второй моделью (`plan-challenge.sh`).

## Главный тезис

Переходим от ручной оркестрации ИИ-сессий к автономной фабрике разработки:
машина забирает наблюдение и исполнение, человек оставляет за собой решения
(ставки, вердикты, приёмку). Система универсальна: один рантайм на машину,
обслуживает все проекты (ai-evolve, betzo, psylia, noproblem).

Обоснование перехода: сегодня системные функции (вотчеры, таймеры, очереди)
живут внутри ИИ-сессий — дорогих и смертных. Диагноз 27.07: вотчеры умирают
от таймаутов каждые 10 минут, очередь мержа держит записи-призраки 5 дней,
человек узнаёт о сбоях раньше диспетчера. Вывод: системные функции надо
вынести из сессий в постоянный рантайм.

Система состоит из трёх частей (MECE): **исполнители** (кто делает работу),
**управление** (кто распределяет и контролирует), **знания** (что направляет
работу и проверяет результат).

## 1. Исполнители: флот из пяти ролей

Принцип: у каждой роли — свой вендор и своя экономика. Дорогая модель не
исполняет дешёвую работу.

Экономическая рамка (позиция постановщика, 27.07):
- деньги вторичны — профит от системы больше её стоимости; на копейках не
  оптимизируемся;
- но **чисто API — запрещено**: при объёмах фабрики API-биллинг разорителен;
  основа — подписки (фикс-кост), API — только переливной клапан и спец-задачи;
- подписки Claude уже насыщены: 4 штук едва хватает на неделю. Следствия:
  масштаб = добавление подписок + вынос всей несложной работы на дешёвые
  роли (QA-модель, Sol); лимит-менеджмент дирижёра (учёт окон, каскад,
  парковка) — не оптимизация, а условие выживания фабрики.

| Роль | Модель | Оплата | Нагрузка |
|---|---|---|---|
| Разработка (диспетчер, волны, код) | Claude — 4 подписки | подписки | пиковая, окнами |
| Кросс-ревью кода и планов | GPT (Sol/Terra) через codex CLI | подписка | по конвейеру |
| Третий голос: консилиум на сложных планах/ревью + механический рефакторинг | GLM (та же подписка, что QA) | подписка | по запросу |
| QA: UJM-прогоны, Playwright, регрессия | GLM (пилот); Kimi — картридж на пересмотр (п. 3.4) | подписка/дёшево | фоновая, 24/7 |
| Спец-задачи (генерация картинок и пр.) | API точечно (Replicate и др.) | pay-per-use | эпизодическая |

Принцип консилиума: у модельных семей разные слепые пятна — на СЛОЖНЫХ
решениях (архитектура, спорные ревью) к паре «Claude пишет — GPT проверяет»
добавляется третья семья (GLM/Kimi) третьим голосом; обычные тикеты идут
парой, как сейчас.

Документацию ведёт та роль, которая произвела изменение, — правкой ИСТОЧНИКА
(репо/vault); Confluence-витрина рендерится из источника CI-пайплайном
(md2conf, поддерево «Docs (auto)», баннер generated + запрет ручных правок).
Отдельного исполнителя-документалиста не заводим. Канон трёх слоёв:
**репо — как устроено; vault — что мы думаем и почему; Confluence — что мы
показываем** (детали — research/2026-07-27-round2-digest.md).

Форм-фактор исполнителей (уточнено 27.07 по факту машины): сегодня — **4–5
клонов Claude.app через Parall.app** (клонировщик приложений: у каждого клона
свой bundle ID `app.parall.mac.*`, своё хранилище и логин; НЕ виртуалки —
машина/IP общие; работает без банов). Целевой
форм-фактор работы — headless CLI-процессы с профилями `CLAUDE_CONFIG_DIR`
на аккаунт, которые демон запускает ЛОКАЛЬНО: это ТА ЖЕ степень изоляции,
что сейчас, риск не растёт. Desktop-копии остаются рабочим местом человека
и запасным контуром. VM (Parallels/UTM) — НЕ текущее состояние, а опция
усиления изоляции на будущее (например, при переезде на ферму или если
fingerprint-риск начнёт кусаться).

## 2. Управление: дирижёр из трёх компонентов

**Имя продукта — STOVP** (решение 27.07): от фамилии основателя (Stovpets;
«стовп» = столб/опора — буквально вышка) + бэкроним **Self-Tuning
Orchestrator for Vendor Pools** — самонастраивающийся оркестратор пулов
вендоров, что и есть суть системы. Компоненты: демон — `stovpd`, веб-морда —
Stovp (она же «вышка» в текстах), CLI — `stovp`. Слаг короткий и свободный —
задел под опенсорс (проверить и застолбить github/npm/домен до первой строки
кода).

Дирижёр — приложение на Mac (Apple Silicon), один на машину. Три компонента,
разделение по функции (MECE): видеть / обеспечивать / запускать.

### 2.1 Stovp (вышка) — наблюдение

Локальный дашборд + алерты в Telegram. Показывает: доску волн/job'ов (пульс,
тикеты, ожидание вердикта), ленту будки, очередь и брони, расход токенов и
квоты по аккаунтам. Окно-транскрипт исполнителя: сообщение из поля ввода
создаёт задачу-продолжение (control-job с `--resume` той же сессии) — это
не «живой чат с процессом», процесс мог уже завершиться. Алертит: тишина
будки, тикет без вердикта, протухший lease/heartbeat. Результат: человек
перестаёт быть системой мониторинга.

### 2.2 Демон — инфраструктура

`stovpd` (бывш. рабочее имя nyron-hubd), launchd-сервис. Владеет будкой и
очередью работ: таблицы jobs/attempts/leases/events (outbox, идемпотентность),
heartbeat исполнителей, TTL и чистка очередей/броней, supervision (CPU-жор,
протухшие lease). Вотчеры и «пробуждения» в целевой архитектуре отсутствуют —
они остаются только как временная гигиена этапа 0 до переезда на job-модель.
Результат: класс багов «сервис умер вместе с сессией» устранён архитектурно.

### 2.3 Запуск флота — исполнение

Дирижёр сам поднимает исполнителей headless-процессами: модель — параметром,
резюм после лимита — автоматический, распределение — по остатку лимитов
подписок. Чипы, пикер модели и ручные подтверждения исчезают из МЕХАНИКИ;
решения (вердикты, приёмка, мерж) остаются у человека. Результат = KPI
этапа 4а: атомарная задача проходит Jira → MR → CI → UJM без ручного пинка,
при сбое — точный алерт и воспроизводимый чекпойнт.

Не входит в управление (осознанно): замена Jira и будки новой шиной
(дорабатываем существующие), GUI-автоматизация Desktop-окон (хрупко).

## 3. Знания: канон, сценарии, петля

### 3.1 Канон ролей — плагин nyron-dev

Скиллы, правила волн, конвейер, конфиг проекта. Остаётся переносимым слоем:
дирижёр читает канон, а не заменяет его.

**Плагин — операционная система команды, не инструмент одного диспетчера.**
Каждый член команды работает в СВОЕЙ зоне через ОБЩИЙ канон: постановщик —
в docs-репо и Jira, без чужих код-зон. Примеры-образцы: Агиль (QA, evolve) —
UJM-каталог; Юра (фичи, nyron) — мокапы на дизайн-системе + фич-реквесты;
Марина (psylia) — своя папка. Плагин систематизирует людей так же, как
агентов: одни стандарты задач, документов и статусов на всех — поэтому
ничего не рассыпается при любом числе участников и проектов.

**Дизайн-система — обязательная часть канона каждого продукта** (агенты
строят UI только из её компонентов — quality_floor это уже энфорсит; плюс
она же — сырьё для быстрых мокапов продактам). Состояние по проектам:
psylia — есть; evolve/nyron — только что построена, улучшается;
betzo — есть «казиношная», кандидат на выделение в переиспользуемую
(бэклог, не сейчас); noproblem — прояснить и завести.

### 3.2 Библиотека UJM (User Journey Map) — исполняемые пути пользователя

Каталог канонических пользовательских путей; путь каталога — per-project в
конфиге плагина (для evolve: `ai-evolve-docs-test/tests/e2e/journeys/`;
этап 2 — пилот на evolve, масштабирование на другие проекты после). Примеры
путей: «логин → создание плейбука →
прохождение оркестратора», «создание компании → роли → несколько юзеров на
доске». Каждый UJM: человекочитаемая карта пути (шапка: что за путь, роли,
данные) + Playwright-код, исполняющий этот путь + фикстуры (тест-юзеры,
тест-компания) + теги окружений. Термин в команде — UJM; «сценарий» —
синоним в тексте.

Потребители (MECE по моменту использования):
- **в разработке** — волна дёргает сценарий вместо самодельной проверки;
- **на приёмке** — диспетчер: смоук после мержа = прогон затронутых сценариев;
- **в фоне** — QA-модель гоняет регрессию в простое; падение → алерт и тикет;
- **для людей** — Агиль владеет каталогом и ревьюит канон; Confluence-слой
  генерится из шапок.

Пополнение: карта флоу из live-epic-testing конвертируется в сценарии; правило
конвейера — фича, меняющая пользовательский путь, обновляет сценарий тем же
тикетом.

### 3.3 Продуктовая петля

Вход и производство уже есть скиллами: intake-groom → product-flow →
jira-task-standard / nyron-waves → qa-handoff. Замыкание: телеметрия
(Langfuse, метрики стенда, UJM-прогоны) автоматически становится замером
гипотезы, система готовит ПРОЕКТ вердикта — утверждает человек;
автоматический вердикт допустим только по заранее заданным в карточке
порогам. Гипотеза — родительская карточка: при утверждении в неё линкуются
dev/design-таски, прогресс и замер трекаются сквозь неё.

### 3.4 QA-модель — решение шторма 27.07: пилот на GLM, Kimi — картридж

Проверка живых прайсов 27.07: **Kimi купить нельзя** (все тарифы в листе
ожидания, сетку пересобирают, квоты «×N» без базы), K3 always-on-reasoning
медленный для рутины. **GLM Coding Plan** покупается сегодня, официально
работает внутри Claude Code (наш харнесс, скилы и MCP без миграции), vision
включён во все планы, Lite $12.6–18/мес (Pro $50.4–72, Max $112–160).

Решение: **недельный пилот на GLM Lite помесячно**; апгрейд до Pro — только
по замеренному расходу. Сценарии пилота: (1) скриншот стенда → написать
UJM-тест; (2) сломанный селектор → чинит ли по trace/скриншоту; (3) ночной
цикл с авторазбором падений; (4) замер сжигания квоты (промпты за 5-часовое
окно — главный риск GLM). Роль = слот, модель = картридж (строка в конфиге):
Kimi пересматриваем, когда откроют продажи Kimi Code отдельно.

## Дорожная карта

Зависимости — граф: **0 → (1 ‖ 2 параллельно) → 3 → 4а → 4б → 5**. Каждый
этап имеет самостоятельную ценность и критерий готовности; зависимый этап
не начинается до критерия своего предшественника по графу.

**Переходный контракт (до этапа 3):** волны работают по текущему протоколу
Desktop-сессий с вотчерами, подлатанными этапом 0 (без timeout, замена
вместо дубля, живой реаниматор); это признанный временный режим, в него не
инвестируем сверх гигиены. Мосты выключаются при переезде на job-модель
(этап 3), не раньше и не позже.

Модель исполнения целевых этапов (решение шторма, применимо к ЛЮБОМУ типу
работы — код, ресёрч, аналитика, контент): **job-модель** — единица работы
лежит в очереди, исполнитель = конечный процесс (взял lease → сделал шаг →
записал чекпойнт → завершился), контекст продолжается через `--resume`,
протухший lease подбирает следующий процесс. Вотчеры/пробуждения/парковки
исчезают как класс; wake-мосты для Desktop-сессий — только этап 0, в целевую
архитектуру не входят. Тип работы определяет адаптеры и способ проверки,
не механику.

| # | Этап | Содержание | Критерий готовности |
|---|---|---|---|
| 0 | Гигиена | синк wave-rules v2.1; запрет timeout на вотчерах/Sol; фиксы hub-watch (замена вместо дубля, busy-флаппинг реаниматора); чистка очереди мержа; чек-аут-лист финала волны; фикс config-template (6 ключей статусов); разовая git-уборка репо; алерт-скрипт «Desktop жрёт CPU при пустой будке» | сутки прогона без глухих сессий и ручных пинков |
| 1 | Вышка | веб-дашборд (SvelteKit) + Telegram-алерты; сенсоры готовые: OTel Claude Code, ccusage, oauth/usage | о застревании, видимом сенсорам (пульс будки, статусы Jira, квоты), узнаём из алерта; полное liveness-покрытие — после этапа 3 |
| 2 | UJM + каноны | каталог UJM (playwright-bdd + Test Agents + reports-server + мультиюзер-фикстуры) + пилот GLM (п. 3.4) + согласование с Агилем; каноны: трекер (psylia-модель: 6-ключевой маппинг, маршруты dev+product, STATUS/ROADMAP-слой), vault (betzo-модель), Confluence-пайплайн (md2conf в CI), CLAUDE.md-стандарт (правила + ручной прогон; фоновый линт — после этапа 3) | волна дёргает UJM вместо самодельного смоука; ночная регрессия идёт; каноны записаны в плагин и применены минимум на одном проекте |
| 3 | Демон | stovpd (Python/FastAPI, портируемое ядро + OS-адаптеры): очередь job'ов с lease/outbox, supervision (CPU-жор, протухшие lease), TTL/GC; фоновые job'ы (git-уборщик, линт CLAUDE.md, переиндексация RAG) | ноль shell-вотчеров в сессиях; смерть сессии не теряет события |
| 4а | Флот: вертикаль | headless-запуск локально по профилям `CLAUDE_CONFIG_DIR` (один профиль); job-конвейер одного типа задачи на одном проекте (пилот Claudexor как исполнительный слой) | **вертикаль-KPI**: атомарная задача проходит Jira → MR → CI → UJM без ручного пинка; при сбое — точный алерт и воспроизводимый чекпойнт |
| 4б | Флот: мультипрофиль | ≥2 профилей, учёт квот, ротация по типизированным limit-сигналам, парковка/каскад | лимит одной подписки не останавливает фабрику (проверено намеренным дожиганием окна) |
| 5 | Петля | телеметрия → замеры гипотез; гипотеза-родитель линкует dev/design-таски | проект вердикта гипотезы формируется из данных без ручного сбора; человек только утверждает |

Порядок — **гибрид** (решение шторма): этапы 1 и 2 ведутся параллельно
(общих компонентов нет, строятся волнами нашего же плагина); 3 → 4а — «узкая
вертикаль» (один проект, один аккаунт, один тип задачи, один UJM), расширение
ролей и проектов — после её надёжности (4б+). Каждое решение проверяется
вопросом «а если демон не на этой машине?» — переезд на выделенную ферму
(Mac mini) обязан быть конфигом, не перестройкой.

## Риски

| Риск | Митигация |
|---|---|
| Разморозка «Agent SDK credit» (headless на подписках переоценят по API-ценам — анонс 15.06.2026 приостановлен) | риск №1 этапа «Флот»: у каждого профиля fallback на API-ключ/интерактив; Desktop-контур в VM сохраняется рабочим |
| Fingerprint-баны мультиаккаунта (одна машина/IP/платёжка) | Parallels-изоляция + опционально свой VPN на VM; разные плательщики; не молотить аккаунты синхронно одинаковой нагрузкой; только официальный CLI/SDK, токены не извлекать |
| Квоты GLM в промптах, не токенах (агентная сессия жжёт быстро) | замер сжигания — обязательный пункт пилота; апгрейд тарифа только по цифрам |
| Мультиаккаунт headless не взлетит (авторизация, ToS) | проверка прогоном; Desktop-окна в тех же VM — фолбэк |
| Desktop-сессии нельзя будить извне | не строить мосты-костыли; полный ответ — этап 4 (job-модель) |
| UJM-каталог без фреймворка выродится | опасность не «нет человека», а «нет правил»: владелец каталога — РОЛЬ (человек или ИИ-сотрудник), живущая в фреймворке (формат, гейты качества, ревью пополнений); Агиль — спот-чек, не узкое место (позиция постановщика 27.07) |
| Решения ломают универсальность | каждое решение проверять на betzo/psylia/noproblem; дирижёр обязан уважать project-config |
| Сползание на API-биллинг («улетим в трубу») | API только как переливной клапан и спец-задачи; любой постоянный поток работы обязан жить на подписке; вышка показывает API-расход отдельной строкой |
| Насыщение подписок Claude (4 еле хватает на неделю) | лимит-менеджмент дирижёра с этапа 4; до него — модельная экономика ролей (Opus/Sonnet/Haiku, QA-модель на фоне) и парковка по канону |
